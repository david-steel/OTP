node_modules/
dist/
.env
.env.local
*.log
.DS_Store
drizzle-kit.config.*
.env.bak
.gstack/
.ijfw/

# Scraper cache (regenerated by npm run scrape:eos)
data/eos-implementers.json

# scratch / preview artifacts (kept on disk, out of git)
.preview/
*.preview.html
*.original.png
*.prev.png

# billing serialization lock (mkdir-atomic, shared across sessions)
.billing-lock/
