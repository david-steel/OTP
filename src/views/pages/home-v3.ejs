<style>
  /* Four-stage arc: tool -> assistant -> agent -> robot
     Stage 3 (Agent) is the current "you are here" -- highlighted as OTP's home.
     Stage 4 (Robot) is the next wave -- subtle glow, "shipping" badge. */
  .stage-card {
    position: relative;
    background: rgba(15, 23, 42, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 14px;
    padding: 1.25rem 1rem;
    transition: transform 250ms ease, border-color 250ms ease, background 250ms ease;
    backdrop-filter: blur(8px);
    -webkit-backdrop-filter: blur(8px);
  }
  .stage-card:hover { transform: translateY(-2px); }
  .stage-marker {
    width: 44px; height: 44px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    margin: 0 auto 0.75rem auto;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.08);
    color: #6b7280;
    z-index: 1;
    position: relative;
  }
  .stage-meta {
    text-align: center;
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: #6b7280;
    font-weight: 600;
    margin-bottom: 0.4rem;
  }
  .stage-name {
    text-align: center;
    font-size: 22px;
    font-weight: 800;
    color: #f3f4f6;
    margin-bottom: 0.5rem;
    letter-spacing: -0.01em;
  }
  .stage-desc {
    text-align: center;
    font-size: 12px;
    line-height: 1.45;
    color: #9ca3af;
  }

  /* Past stages: muted, history */
  .stage-past .stage-marker { color: #6b7280; }
  .stage-past .stage-name { color: #9ca3af; }
  .stage-recent .stage-marker { color: #818cf8; background: rgba(99, 102, 241, 0.06); border-color: rgba(99, 102, 241, 0.18); }
  .stage-recent .stage-name { color: #c7d2fe; }

  /* Current stage (Agent) -- highlighted, brighter, pulse */
  .stage-current {
    background: linear-gradient(180deg, rgba(14, 165, 233, 0.10) 0%, rgba(15, 23, 42, 0.4) 100%);
    border-color: rgba(14, 165, 233, 0.35);
    box-shadow: 0 0 32px rgba(14, 165, 233, 0.10);
  }
  .stage-current .stage-name { color: #fff; }
  .stage-current .stage-desc { color: #cbd5e1; }
  .stage-marker-current {
    color: #fff !important;
    background: linear-gradient(135deg, #0ea5e9 0%, #0284c7 100%) !important;
    border-color: rgba(56, 189, 248, 0.6) !important;
    box-shadow: 0 0 0 4px rgba(14, 165, 233, 0.18), 0 0 24px rgba(14, 165, 233, 0.45);
    animation: stage-pulse 2.6s ease-in-out infinite;
  }
  @keyframes stage-pulse {
    0%, 100% { box-shadow: 0 0 0 4px rgba(14, 165, 233, 0.18), 0 0 24px rgba(14, 165, 233, 0.45); }
    50% { box-shadow: 0 0 0 8px rgba(14, 165, 233, 0.10), 0 0 36px rgba(14, 165, 233, 0.65); }
  }
  .stage-here-pill {
    position: absolute; top: -10px; left: 50%; transform: translateX(-50%);
    display: inline-flex; align-items: center; gap: 4px;
    padding: 2px 10px; border-radius: 999px;
    font-size: 9px; font-weight: 700; letter-spacing: 0.04em; text-transform: uppercase;
    background: rgba(16, 185, 129, 0.15);
    color: #6ee7b7;
    border: 1px solid rgba(16, 185, 129, 0.35);
    white-space: nowrap;
  }

  /* Future stage (Robot) -- gathering, faint purple glow, "shipping" tone */
  .stage-future {
    background: linear-gradient(180deg, rgba(168, 85, 247, 0.06) 0%, rgba(15, 23, 42, 0.4) 100%);
    border-color: rgba(168, 85, 247, 0.18);
  }
  .stage-marker-future {
    color: #c4b5fd !important;
    background: rgba(168, 85, 247, 0.12) !important;
    border-color: rgba(168, 85, 247, 0.32) !important;
    box-shadow: 0 0 16px rgba(168, 85, 247, 0.18);
  }
  .stage-future .stage-name { color: #e9d5ff; }
</style>

<!-- ============================================================
     HERO -- editorial, asymmetric, first-person.
     No canvas particles, no orbs, no slider, no animate-pulse dots.
     The shape: top label, big declarative headline left, real-looking
     chart panel right, founder copy + two text-link CTAs, then a thin
     border, then a pull quote with attribution and link.
     ============================================================ -->
<section class="bg-otp-950 relative">
  <!-- Faint grid lines instead of orbs. Decorative but quiet. -->
  <div class="absolute inset-0 pointer-events-none opacity-[0.04]" style="background-image: linear-gradient(to right, rgba(255,255,255,0.4) 1px, transparent 1px), linear-gradient(to bottom, rgba(255,255,255,0.4) 1px, transparent 1px); background-size: 80px 80px;"></div>

  <div class="relative max-w-6xl mx-auto px-6 sm:px-8 pt-28 sm:pt-32 pb-16 sm:pb-20">

    <!-- Top label -->
    <div class="text-[11px] text-gray-500 tracking-[0.18em] uppercase mb-12 sm:mb-16 font-medium">
      OTP &nbsp;/&nbsp; operating protocol for the augmented human organization
    </div>

    <!-- Asymmetric grid: text 7 / chart 5 -->
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-10 lg:gap-14 items-start">

      <!-- Left: confessional opener, pivot to OTP, one paragraph that
           defines OTP and uses Sneeze It as the named exhibit. Voice
           stays first-person. OTP is the protagonist. -->
      <div class="lg:col-span-7">
        <h1 class="font-extrabold text-white leading-[1.0] tracking-tight" style="font-size: clamp(2.5rem, 6.4vw, 4.75rem);">
          Half my company is&nbsp;AI.<br>
          <span class="text-gray-500">That's the reason OTP exists.</span>
        </h1>

        <p class="text-lg text-gray-300 leading-[1.6] max-w-xl mt-10 mb-6">
          OTP is the operating layer where humans and AI agents share seats, share SOPs, and share one scoreboard. The chart on the right is a working OTP chart. It belongs to Sneeze It, the agency I run, where fourteen AI agents hold seats next to twelve humans. Before OTP, this was tabs in my browser. Now it's an org.
        </p>

        <p class="text-sm text-gray-500 mb-10">— David Steel, founder of OTP</p>

        <div class="flex flex-wrap gap-x-8 gap-y-3 items-center">
          <a href="/sign-up" class="text-white border-b border-white pb-1 text-base font-medium hover:text-otp-300 hover:border-otp-300 transition-colors">
            Build your chart →
          </a>
          <a href="/what-is-otp" class="text-gray-400 border-b border-white/15 pb-1 text-base font-medium hover:text-white hover:border-white/40 transition-colors">
            What is OTP →
          </a>
        </div>
      </div>

      <!-- Right: chart panel, refined, with real names + slight off-grid feel -->
      <div class="lg:col-span-5 relative">
        <div class="hero-chart-panel">
          <!-- Tiny header strip -->
          <div class="flex items-center justify-between text-[10px] text-gray-500 uppercase tracking-[0.14em] mb-5 font-medium">
            <span>An OTP chart &nbsp;·&nbsp; <span class="text-gray-600">Sneeze&nbsp;It</span></span>
            <span class="flex items-center gap-1.5"><span class="hero-live-dot"></span>live</span>
          </div>

          <!-- Tiles: David at top, then Bogdan + Pepper, then row of agents -->
          <div class="hero-row hero-row-top">
            <div class="hero-tile hero-tile-h">
              <div class="hero-tile-stripe" style="background:#f59e0b;"></div>
              <div class="hero-tile-name">David Steel</div>
              <div class="hero-tile-role">CEO</div>
            </div>
          </div>

          <div class="hero-connector"></div>

          <div class="hero-row hero-row-mid">
            <div class="hero-tile hero-tile-h">
              <div class="hero-tile-stripe" style="background:#f59e0b;"></div>
              <div class="hero-tile-name">Bogdan</div>
              <div class="hero-tile-role">COO</div>
            </div>
            <div class="hero-tile hero-tile-a">
              <div class="hero-tile-stripe" style="background:#0ea5e9;"></div>
              <div class="hero-tile-name">Pepper</div>
              <div class="hero-tile-role">Inbox</div>
              <div class="hero-tile-meta">12&nbsp;SOPs</div>
            </div>
          </div>

          <div class="hero-row hero-row-bot">
            <div class="hero-tile hero-tile-a hero-tile-sm">
              <div class="hero-tile-stripe" style="background:#0ea5e9;"></div>
              <div class="hero-tile-name">Crystal</div>
              <div class="hero-tile-role">PM</div>
            </div>
            <div class="hero-tile hero-tile-a hero-tile-sm">
              <div class="hero-tile-stripe" style="background:#0ea5e9;"></div>
              <div class="hero-tile-name">Dirk</div>
              <div class="hero-tile-role">Sales</div>
              <div class="hero-tile-kpi">86 / 150</div>
            </div>
            <div class="hero-tile hero-tile-a hero-tile-sm">
              <div class="hero-tile-stripe" style="background:#0ea5e9;"></div>
              <div class="hero-tile-name">Tally</div>
              <div class="hero-tile-role">Scorecard</div>
            </div>
            <div class="hero-tile hero-tile-a hero-tile-sm">
              <div class="hero-tile-stripe" style="background:#0ea5e9;"></div>
              <div class="hero-tile-name">Dash</div>
              <div class="hero-tile-role">Analytics</div>
            </div>
          </div>

          <div class="text-[10px] text-gray-500 mt-5 leading-relaxed">
            On OTP, every agent reports its own KPI. <strong class="text-gray-300">Dirk</strong> just pushed 86 of 150 cold emails for the week. The chart is wired to runtime.
          </div>
        </div>
      </div>

    </div>
  </div>

  <!-- Pull quote pulled 2026-05-02: Sean Donahoe's GitHub presence is gone
       (account + repos all 404). Quote was real but the link is dead and
       the attribution is awkward without a working source. The space is
       held -- a different proof point can land here later. -->
</section>

<style>
  /* Hero chart panel -- mirrors /dashboard/team's tile idiom but slim */
  .hero-chart-panel {
    background: linear-gradient(180deg, rgba(15,23,42,0.45) 0%, rgba(15,23,42,0.30) 100%);
    border: 1px solid rgba(255,255,255,0.07);
    border-radius: 12px;
    padding: 18px 20px 20px 20px;
    box-shadow: 0 30px 80px -20px rgba(0,0,0,0.45);
  }
  .hero-row { display: flex; gap: 12px; justify-content: center; }
  .hero-row-top { justify-content: center; margin-bottom: 4px; }
  .hero-row-mid { justify-content: center; gap: 22px; margin: 0 0 4px 0; }
  .hero-row-bot { gap: 8px; }
  .hero-connector {
    width: 1px; height: 18px; background: rgba(255,255,255,0.18); margin: 0 auto;
  }
  .hero-tile {
    position: relative;
    background: rgba(15,23,42,0.85);
    border: 1px solid rgba(255,255,255,0.09);
    border-radius: 8px;
    padding: 10px 12px 10px 14px;
    min-width: 130px;
    overflow: hidden;
  }
  .hero-tile-sm { min-width: 0; flex: 1; padding: 8px 10px 8px 12px; }
  .hero-tile-stripe {
    position: absolute; top: 0; left: 0; bottom: 0; width: 3px;
  }
  .hero-tile-name { font-size: 12px; font-weight: 700; color: #fff; line-height: 1.2; }
  .hero-tile-role { font-size: 10px; color: #94a3b8; margin-top: 2px; }
  .hero-tile-meta {
    display: inline-block; margin-top: 6px;
    font-size: 9px; font-weight: 700; padding: 2px 6px; border-radius: 999px;
    background: rgba(139,92,246,0.14); color: #c4b5fd;
  }
  .hero-tile-kpi {
    display: inline-block; margin-top: 6px;
    font-size: 9px; font-weight: 700; padding: 2px 6px; border-radius: 4px;
    background: rgba(190,24,93,0.18); color: #f9a8d4;
    border: 1px solid rgba(244,114,182,0.22);
    font-variant-numeric: tabular-nums;
  }
  .hero-live-dot {
    width: 5px; height: 5px; border-radius: 50%; background: #10b981;
    box-shadow: 0 0 0 3px rgba(16,185,129,0.18);
  }
</style>



<!-- ============================================================
     FIVE FRUSTRATIONS -- pain pivot, in the visitor's own voice.
     Editorial layout, overheard-quote style. White, narrow column.
     ============================================================ -->
<section class="bg-white py-20 sm:py-28 border-t border-gray-100">
  <div class="max-w-4xl mx-auto px-6 sm:px-8">
    <div class="text-[11px] text-gray-500 tracking-[0.18em] uppercase mb-4 font-medium">
      Why running an AI team feels so hard
    </div>
    <h2 class="text-3xl sm:text-5xl font-extrabold text-gray-900 leading-[1.08] tracking-tight max-w-3xl mb-12">
      Five things you're probably saying about<br class="hidden sm:block">
      your AI team right&nbsp;now.
    </h2>

    <div class="space-y-7 max-w-3xl">

      <div class="grid grid-cols-12 gap-5 items-baseline">
        <div class="col-span-1 text-2xl font-extrabold text-gray-300 tracking-tight" style="font-variant-numeric: tabular-nums;">1</div>
        <blockquote class="col-span-11 text-xl sm:text-2xl text-gray-900 leading-[1.4] font-light italic" style="font-style: italic;">
          "My AI tools are scattered across 14 tabs."
        </blockquote>
      </div>

      <div class="grid grid-cols-12 gap-5 items-baseline">
        <div class="col-span-1 text-2xl font-extrabold text-gray-300 tracking-tight" style="font-variant-numeric: tabular-nums;">2</div>
        <blockquote class="col-span-11 text-xl sm:text-2xl text-gray-900 leading-[1.4] font-light italic" style="font-style: italic;">
          "My senior people are drowning at hire seven."
        </blockquote>
      </div>

      <div class="grid grid-cols-12 gap-5 items-baseline">
        <div class="col-span-1 text-2xl font-extrabold text-gray-300 tracking-tight" style="font-variant-numeric: tabular-nums;">3</div>
        <blockquote class="col-span-11 text-xl sm:text-2xl text-gray-900 leading-[1.4] font-light italic" style="font-style: italic;">
          "The SOPs we wrote last quarter are already ignored."
        </blockquote>
      </div>

      <div class="grid grid-cols-12 gap-5 items-baseline">
        <div class="col-span-1 text-2xl font-extrabold text-gray-300 tracking-tight" style="font-variant-numeric: tabular-nums;">4</div>
        <blockquote class="col-span-11 text-xl sm:text-2xl text-gray-900 leading-[1.4] font-light italic" style="font-style: italic;">
          "I genuinely don't know what each agent is actually doing."
        </blockquote>
      </div>

      <div class="grid grid-cols-12 gap-5 items-baseline">
        <div class="col-span-1 text-2xl font-extrabold text-gray-300 tracking-tight" style="font-variant-numeric: tabular-nums;">5</div>
        <blockquote class="col-span-11 text-xl sm:text-2xl text-gray-900 leading-[1.4] font-light italic" style="font-style: italic;">
          "Every new agent gets re-explained from scratch."
        </blockquote>
      </div>

    </div>

    <div class="mt-14 max-w-3xl">
      <p class="text-base text-gray-600 leading-[1.7] mb-6">
        These are not five separate problems. They are five symptoms of the same thing: your AI is doing real work but it does not have a seat. OTP gives every agent a seat, an SOP, and a scorecard, on the same chart as your humans.
      </p>
      <a href="/what-is-otp" class="text-gray-900 border-b border-gray-900 pb-1 text-base font-semibold hover:text-otp-600 hover:border-otp-600 transition-colors">
        How OTP solves the five frustrations →
      </a>
    </div>
  </div>
</section>

<!-- ============================================================
     HOW OTP WORKS -- three pillars, lighter than /what-is-otp's six.
     Bold typography, no icons. Each pillar is one big word + one
     short paragraph + one inline link.
     ============================================================ -->
<section class="bg-gray-50 py-20 sm:py-28 border-t border-gray-100">
  <div class="max-w-5xl mx-auto px-6 sm:px-8">
    <div class="text-[11px] text-gray-500 tracking-[0.18em] uppercase mb-4 font-medium">
      How OTP works
    </div>
    <h2 class="text-3xl sm:text-5xl font-extrabold text-gray-900 leading-[1.08] tracking-tight max-w-3xl mb-14">
      Three pieces that make a hybrid org actually run.
    </h2>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 lg:gap-12">

      <div>
        <div class="text-5xl font-extrabold text-gray-900 leading-none tracking-tight mb-5">Chart.</div>
        <p class="text-base text-gray-700 leading-[1.7] mb-4">
          Every seat &mdash; human or AI &mdash; named, with a role, an authority level, and an accountability line. The chart is where the hybrid shape becomes legible.
        </p>
        <p class="text-sm text-gray-500 leading-[1.6]">
          Bigger orgs cannot retrofit this; their coordination layer is political, not protocol.
        </p>
      </div>

      <div>
        <div class="text-5xl font-extrabold text-gray-900 leading-none tracking-tight mb-5">Inheritance.</div>
        <p class="text-base text-gray-700 leading-[1.7] mb-4">
          SOPs are runtime context, not documents. The agent reads them at every session. The human edits at noon, the agent that boots at 12:01 has the new version.
        </p>
        <p class="text-sm text-gray-500 leading-[1.6]">
          The doc and the work converge. No more rollout half-life.
        </p>
      </div>

      <div>
        <div class="text-5xl font-extrabold text-gray-900 leading-none tracking-tight mb-5">Network.</div>
        <p class="text-base text-gray-700 leading-[1.7] mb-4">
          Every organization that publishes its OOS makes every other org smarter. Coordination intelligence compounds across the network the way knowledge compounds on Wikipedia.
        </p>
        <p class="text-sm text-gray-500 leading-[1.6]">
          You get smarter because the orgs ahead of you publish what they figured out.
        </p>
      </div>

    </div>

    <div class="mt-14">
      <a href="/what-is-otp" class="text-gray-900 border-b border-gray-900 pb-1 text-base font-semibold hover:text-otp-600 hover:border-otp-600 transition-colors">
        See the full model &mdash; six components &rarr;
      </a>
    </div>
  </div>
</section>
<!-- THE SCORE: Coordination Score -->
<section class="relative overflow-hidden bg-otp-950 py-20 sm:py-32">
  <div class="absolute inset-0 bg-grid opacity-30"></div>
  <div class="absolute top-[-150px] right-[-100px] w-[500px] h-[500px] bg-otp-500/10 rounded-full blur-[140px] pointer-events-none"></div>

  <div class="relative max-w-6xl mx-auto px-4 sm:px-6">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-20 items-center">

      <!-- Left: Score ring visualization -->
      <div class="reveal flex justify-center lg:justify-start">
        <div class="relative">
          <!-- Outer glow -->
          <div class="absolute -inset-8 bg-otp-500/5 rounded-full blur-3xl"></div>

          <div class="relative w-[300px] h-[300px] sm:w-[360px] sm:h-[360px]">
            <!-- SVG Score Ring -->
            <svg class="w-full h-full" viewBox="0 0 360 360">
              <!-- Background ring -->
              <circle cx="180" cy="180" r="140" fill="none" stroke="rgba(255,255,255,0.05)" stroke-width="20"/>
              <!-- Score arc (82/100 = ~295 degrees) -->
              <circle cx="180" cy="180" r="140" fill="none" stroke="url(#scoreGradient)" stroke-width="20" stroke-linecap="round"
                stroke-dasharray="879.6" stroke-dashoffset="158.3"
                transform="rotate(-90 180 180)"
                class="score-ring"/>
              <!-- Gradient definition -->
              <defs>
                <linearGradient id="scoreGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                  <stop offset="0%" stop-color="#0095ff"/>
                  <stop offset="50%" stop-color="#7c3aed"/>
                  <stop offset="100%" stop-color="#0095ff"/>
                </linearGradient>
              </defs>
              <!-- Center score -->
              <text x="180" y="170" text-anchor="middle" fill="white" font-size="72" font-weight="800" font-family="Inter, sans-serif">82</text>
              <text x="180" y="200" text-anchor="middle" fill="rgba(255,255,255,0.4)" font-size="14" font-weight="500" font-family="Inter, sans-serif">Coordination Score</text>
            </svg>

            <!-- Dimension bars around the ring -->
            <div class="absolute bottom-[-40px] left-1/2 -translate-x-1/2 flex gap-2">
              <div class="flex flex-col items-center gap-1">
                <div class="w-8 h-20 bg-white/5 rounded-md overflow-hidden flex flex-col-reverse"><div class="bg-otp-500/70 rounded-sm" style="height: 85%"></div></div>
                <span class="text-[8px] text-gray-600 font-medium">CF</span>
              </div>
              <div class="flex flex-col items-center gap-1">
                <div class="w-8 h-20 bg-white/5 rounded-md overflow-hidden flex flex-col-reverse"><div class="bg-purple-500/70 rounded-sm" style="height: 90%"></div></div>
                <span class="text-[8px] text-gray-600 font-medium">ES</span>
              </div>
              <div class="flex flex-col items-center gap-1">
                <div class="w-8 h-20 bg-white/5 rounded-md overflow-hidden flex flex-col-reverse"><div class="bg-otp-400/70 rounded-sm" style="height: 75%"></div></div>
                <span class="text-[8px] text-gray-600 font-medium">WC</span>
              </div>
              <div class="flex flex-col items-center gap-1">
                <div class="w-8 h-20 bg-white/5 rounded-md overflow-hidden flex flex-col-reverse"><div class="bg-green-500/70 rounded-sm" style="height: 80%"></div></div>
                <span class="text-[8px] text-gray-600 font-medium">HO</span>
              </div>
              <div class="flex flex-col items-center gap-1">
                <div class="w-8 h-20 bg-white/5 rounded-md overflow-hidden flex flex-col-reverse"><div class="bg-amber-500/70 rounded-sm" style="height: 70%"></div></div>
                <span class="text-[8px] text-gray-600 font-medium">SR</span>
              </div>
              <div class="flex flex-col items-center gap-1">
                <div class="w-8 h-20 bg-white/5 rounded-md overflow-hidden flex flex-col-reverse"><div class="bg-red-400/70 rounded-sm" style="height: 88%"></div></div>
                <span class="text-[8px] text-gray-600 font-medium">AB</span>
              </div>
            </div>
          </div>
        </div>
      </div>

      <!-- Right: Copy -->
      <div class="reveal delay-200">
        <h2 class="text-3xl sm:text-4xl font-extrabold text-white mb-6 leading-tight">Your Coordination Score</h2>
        <p class="text-gray-400 text-lg leading-relaxed mb-10">See how well your agent team is structured. Six dimensions. One number. The gaps you didn't know you had.</p>

        <div class="space-y-4 mb-10">
          <div class="flex items-center gap-4 group">
            <div class="w-10 h-10 rounded-xl bg-otp-500/10 border border-otp-500/20 flex items-center justify-center shrink-0">
              <svg class="w-5 h-5 text-otp-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/></svg>
            </div>
            <div>
              <div class="text-sm font-semibold text-white">Conflict Management</div>
              <div class="text-xs text-gray-500">How agents handle overlapping responsibilities</div>
            </div>
          </div>
          <div class="flex items-center gap-4 group">
            <div class="w-10 h-10 rounded-xl bg-purple-500/10 border border-purple-500/20 flex items-center justify-center shrink-0">
              <svg class="w-5 h-5 text-purple-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M7 11l5-5m0 0l5 5m-5-5v12"/></svg>
            </div>
            <div>
              <div class="text-sm font-semibold text-white">Escalation Structure</div>
              <div class="text-xs text-gray-500">Clear paths when agents need human decisions</div>
            </div>
          </div>
          <div class="flex items-center gap-4 group">
            <div class="w-10 h-10 rounded-xl bg-otp-400/10 border border-otp-400/20 flex items-center justify-center shrink-0">
              <svg class="w-5 h-5 text-otp-300" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/></svg>
            </div>
            <div>
              <div class="text-sm font-semibold text-white">Workflow Clarity</div>
              <div class="text-xs text-gray-500">Defined processes, inputs, outputs for each agent</div>
            </div>
          </div>
          <div class="flex items-center gap-4 group">
            <div class="w-10 h-10 rounded-xl bg-green-500/10 border border-green-500/20 flex items-center justify-center shrink-0">
              <svg class="w-5 h-5 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"/></svg>
            </div>
            <div>
              <div class="text-sm font-semibold text-white">Human Oversight</div>
              <div class="text-xs text-gray-500">Approval gates and audit trails that work</div>
            </div>
          </div>
          <div class="flex items-center gap-4 group">
            <div class="w-10 h-10 rounded-xl bg-amber-500/10 border border-amber-500/20 flex items-center justify-center shrink-0">
              <svg class="w-5 h-5 text-amber-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/></svg>
            </div>
            <div>
              <div class="text-sm font-semibold text-white">System Redundancy</div>
              <div class="text-xs text-gray-500">Fallbacks when agents or systems go down</div>
            </div>
          </div>
          <div class="flex items-center gap-4 group">
            <div class="w-10 h-10 rounded-xl bg-red-500/10 border border-red-500/20 flex items-center justify-center shrink-0">
              <svg class="w-5 h-5 text-red-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/></svg>
            </div>
            <div>
              <div class="text-sm font-semibold text-white">Authority Boundaries</div>
              <div class="text-xs text-gray-500">Who can do what. No ambiguity.</div>
            </div>
          </div>
        </div>

        <a href="/agent-builder" class="inline-flex items-center gap-2 px-6 py-3 bg-otp-500 text-white rounded-xl font-semibold hover:bg-otp-400 transition-all hover:shadow-lg hover:shadow-otp-500/25 text-sm group">
          Build your agent
          <svg class="w-4 h-4 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"/></svg>
        </a>
      </div>
    </div>
  </div>
</section>


<!-- THE PROOF: Sneeze It case study -->
<section class="bg-white py-20 sm:py-32">
  <div class="max-w-4xl mx-auto px-4 sm:px-6">
    <div class="reveal text-center mb-16">
      <p class="text-sm font-semibold text-otp-500 uppercase tracking-widest mb-4">The origin story</p>
      <h2 class="text-3xl sm:text-4xl font-extrabold text-gray-900 mb-6 leading-tight">We built this because we needed it.</h2>
      <p class="text-lg text-gray-500 leading-relaxed max-w-2xl mx-auto">Sneeze It runs 14 AI agents in daily operations. Our Coordination Score was 68 when we first scanned. We found 6 gaps we didn't know existed. We fixed them. Score: 91. Our agents haven't stepped on each other since.</p>
    </div>

    <!-- Before/After rings -->
    <div class="reveal delay-200 flex flex-col sm:flex-row items-center justify-center gap-8 sm:gap-16">
      <!-- Before -->
      <div class="text-center">
        <div class="relative w-[180px] h-[180px] mx-auto mb-4">
          <svg class="w-full h-full" viewBox="0 0 180 180">
            <circle cx="90" cy="90" r="70" fill="none" stroke="#f3f4f6" stroke-width="12"/>
            <circle cx="90" cy="90" r="70" fill="none" stroke="#d1d5db" stroke-width="12" stroke-linecap="round"
              stroke-dasharray="439.8" stroke-dashoffset="140.7"
              transform="rotate(-90 90 90)"/>
            <text x="90" y="85" text-anchor="middle" fill="#6b7280" font-size="40" font-weight="800" font-family="Inter, sans-serif">68</text>
            <text x="90" y="105" text-anchor="middle" fill="#9ca3af" font-size="10" font-weight="500" font-family="Inter, sans-serif">Before</text>
          </svg>
        </div>
        <p class="text-sm text-gray-400">6 hidden gaps</p>
      </div>

      <!-- Arrow -->
      <div class="flex items-center">
        <svg class="w-12 h-12 text-otp-500 rotate-90 sm:rotate-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"/>
        </svg>
      </div>

      <!-- After -->
      <div class="text-center">
        <div class="relative w-[180px] h-[180px] mx-auto mb-4">
          <svg class="w-full h-full" viewBox="0 0 180 180">
            <circle cx="90" cy="90" r="70" fill="none" stroke="#f3f4f6" stroke-width="12"/>
            <circle cx="90" cy="90" r="70" fill="none" stroke="url(#afterGradient)" stroke-width="12" stroke-linecap="round"
              stroke-dasharray="439.8" stroke-dashoffset="39.6"
              transform="rotate(-90 90 90)"/>
            <defs>
              <linearGradient id="afterGradient" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" stop-color="#0095ff"/>
                <stop offset="100%" stop-color="#7c3aed"/>
              </linearGradient>
            </defs>
            <text x="90" y="85" text-anchor="middle" fill="#111827" font-size="40" font-weight="800" font-family="Inter, sans-serif">91</text>
            <text x="90" y="105" text-anchor="middle" fill="#6b7280" font-size="10" font-weight="500" font-family="Inter, sans-serif">After</text>
          </svg>
        </div>
        <p class="text-sm text-otp-500 font-medium">Zero coordination failures</p>
      </div>
    </div>
  </div>
</section>


<!-- ============================================================
     TOOLBOX PREVIEW -- 5 foundational tools, smaller cards than /tools.
     Each tool has number, title, one-liner, action link.
     Footer links to the full toolbox.
     ============================================================ -->
<section class="bg-gray-50 py-20 sm:py-28 border-t border-gray-100">
  <div class="max-w-5xl mx-auto px-6 sm:px-8">
    <div class="text-[11px] text-gray-500 tracking-[0.18em] uppercase mb-4 font-medium">
      The OTP Toolbox
    </div>
    <h2 class="text-3xl sm:text-5xl font-extrabold text-gray-900 leading-[1.08] tracking-tight max-w-3xl mb-4">
      Five free tools.<br>
      <span class="text-gray-500">Use whichever you reach for first.</span>
    </h2>
    <p class="text-base sm:text-lg text-gray-600 leading-[1.6] max-w-2xl mb-12">
      The protocol is open. The chart is the canonical implementation. Everything below is something a real OTP user reaches for in their first week.
    </p>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mb-10">

      <div class="bg-white border border-gray-200 rounded-xl p-5 hover:border-gray-300 transition-colors flex flex-col">
        <div class="text-xl font-extrabold text-otp-500 tracking-tight mb-3" style="font-variant-numeric: tabular-nums;">01</div>
        <div class="text-base font-bold text-gray-900 mb-2">OOS Starter Template</div>
        <p class="text-xs text-gray-600 leading-[1.6] mb-4 flex-1">
          A commented YAML you fill in to describe your organization. Three KB, ready to publish via the dashboard.
        </p>
        <a href="/public/downloads/oos-starter.yaml" download class="text-otp-600 text-xs font-semibold hover:text-otp-700 inline-flex items-center gap-1.5">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3"/></svg>
          Download
        </a>
      </div>

      <div class="bg-white border border-gray-200 rounded-xl p-5 hover:border-gray-300 transition-colors flex flex-col">
        <div class="text-xl font-extrabold text-otp-500 tracking-tight mb-3" style="font-variant-numeric: tabular-nums;">02</div>
        <div class="text-base font-bold text-gray-900 mb-2">Org Chart Builder</div>
        <p class="text-xs text-gray-600 leading-[1.6] mb-4 flex-1">
          Add humans and AI agents as named seats. Author SOPs. Track KPIs. Export inherited CLAUDE.md.
        </p>
        <a href="/sign-up" class="text-otp-600 text-xs font-semibold hover:text-otp-700 inline-flex items-center gap-1.5">
          Open the builder &rarr;
        </a>
      </div>

      <div class="bg-white border border-gray-200 rounded-xl p-5 hover:border-gray-300 transition-colors flex flex-col">
        <div class="text-xl font-extrabold text-otp-500 tracking-tight mb-3" style="font-variant-numeric: tabular-nums;">03</div>
        <div class="text-base font-bold text-gray-900 mb-2">CLAUDE.md Compiler</div>
        <p class="text-xs text-gray-600 leading-[1.6] mb-4 flex-1">
          Click an agent tile, get a markdown bundle of its SOPs plus inherited SOPs. Works with Claude Code, Cursor, Codex.
        </p>
        <a href="/public/downloads/claude-md-template.md" download class="text-otp-600 text-xs font-semibold hover:text-otp-700 inline-flex items-center gap-1.5">
          <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 16.5v2.25A2.25 2.25 0 0 0 5.25 21h13.5A2.25 2.25 0 0 0 21 18.75V16.5M16.5 12 12 16.5m0 0L7.5 12m4.5 4.5V3"/></svg>
          Download template
        </a>
      </div>

      <div class="bg-white border border-gray-200 rounded-xl p-5 hover:border-gray-300 transition-colors flex flex-col">
        <div class="text-xl font-extrabold text-otp-500 tracking-tight mb-3" style="font-variant-numeric: tabular-nums;">04</div>
        <div class="text-base font-bold text-gray-900 mb-2">Agent Builder</div>
        <p class="text-xs text-gray-600 leading-[1.6] mb-4 flex-1">
          Describe a role, set the personality, get a full agent spec. Thirty seconds. No sign-up.
        </p>
        <a href="/agent-builder" class="text-otp-600 text-xs font-semibold hover:text-otp-700 inline-flex items-center gap-1.5">
          Open the builder &rarr;
        </a>
      </div>

      <div class="bg-white border border-gray-200 rounded-xl p-5 hover:border-gray-300 transition-colors flex flex-col">
        <div class="text-xl font-extrabold text-otp-500 tracking-tight mb-3" style="font-variant-numeric: tabular-nums;">05</div>
        <div class="text-base font-bold text-gray-900 mb-2">Coordination Checkup</div>
        <p class="text-xs text-gray-600 leading-[1.6] mb-4 flex-1">
          Score yourself against the 8 Levels of Agentic Maturity. Ten minutes, gives a number, gives a roadmap.
        </p>
        <a href="/blog/8-levels-of-agentic-maturity" class="text-otp-600 text-xs font-semibold hover:text-otp-700 inline-flex items-center gap-1.5">
          Take the checkup &rarr;
        </a>
      </div>

      <div class="bg-gray-100 border border-dashed border-gray-300 rounded-xl p-5 hover:border-gray-400 transition-colors flex flex-col justify-center items-start">
        <div class="text-xs text-gray-500 uppercase tracking-[0.14em] font-medium mb-3">+ ten more</div>
        <p class="text-sm text-gray-700 leading-[1.55] mb-4">
          Industry packs, OOS browser, claim search, consultant network, agent onboarding, and more.
        </p>
        <a href="/tools" class="text-gray-900 text-sm font-semibold border-b border-gray-900 pb-0.5 hover:text-otp-600 hover:border-otp-600 transition-colors inline-flex items-center gap-1.5">
          Explore the full toolbox &rarr;
        </a>
      </div>

    </div>
  </div>
</section>

<!-- ============================================================
     FINAL CTA -- clean, two doors, no cinematic dark hero clone.
     White background, large headline, two text-link CTAs.
     ============================================================ -->
<section class="bg-white py-20 sm:py-32 border-t border-gray-100">
  <div class="max-w-3xl mx-auto px-6 sm:px-8">
    <h2 class="text-3xl sm:text-5xl font-extrabold text-gray-900 leading-[1.08] tracking-tight mb-6">
      Two doors. Pick the one that fits.
    </h2>
    <p class="text-base sm:text-lg text-gray-600 leading-[1.6] mb-12 max-w-2xl">
      Either you build a chart and run on OTP this week, or you read the four-stage arc that explains why the chart matters in the first place.
    </p>
    <div class="flex flex-col sm:flex-row gap-x-10 gap-y-4 items-start">
      <a href="/sign-up" class="text-gray-900 border-b border-gray-900 pb-1 text-base font-medium hover:text-otp-600 hover:border-otp-600 transition-colors">
        Build your chart &rarr;
      </a>
      <a href="/blog/the-robots-are-coming" class="text-gray-500 border-b border-gray-300 pb-1 text-base font-medium hover:text-gray-900 hover:border-gray-900 transition-colors">
        Read the four-stage arc &rarr;
      </a>
      <a href="/what-is-otp" class="text-gray-500 border-b border-gray-300 pb-1 text-base font-medium hover:text-gray-900 hover:border-gray-900 transition-colors">
        What is OTP &rarr;
      </a>
    </div>
  </div>
</section>
<!-- Disclaimer -->
<div class="bg-otp-950 border-t border-white/5 py-4">
  <p class="text-center text-[10px] text-gray-600 max-w-2xl mx-auto px-4">OTP is an independent platform. Best practices in the library are sourced from publicly available content and attributed to their original authors. OTP is not affiliated with, endorsed by, or sponsored by any third-party organizations referenced in the best practices library.</p>
</div>

<!-- ============================================ -->
<!-- SCRIPTS -->
<!-- ============================================ -->


<!-- Bottom CTA canvas (subtle version) -->
<script>
(function() {
  var canvas = document.getElementById('bottom-canvas');
  if (!canvas) return;
  var ctx = canvas.getContext('2d');
  var dpr = window.devicePixelRatio || 1;
  var W, H, particles = [];

  function resize() {
    var r = canvas.parentElement.getBoundingClientRect();
    W = r.width; H = r.height;
    canvas.width = W * dpr; canvas.height = H * dpr;
    canvas.style.width = W + 'px'; canvas.style.height = H + 'px';
    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
  }

  function init() {
    particles = [];
    for (var i = 0; i < 40; i++) {
      particles.push({
        x: Math.random() * W, y: Math.random() * H,
        vx: (Math.random() - 0.5) * 0.3, vy: (Math.random() - 0.5) * 0.3,
        r: 0.8 + Math.random() * 1,
        color: Math.random() > 0.5 ? '0,149,255' : '124,58,237'
      });
    }
  }

  resize(); init();
  window.addEventListener('resize', function() { resize(); init(); });

  function draw() {
    ctx.clearRect(0, 0, W, H);
    for (var i = 0; i < particles.length; i++) {
      var p = particles[i];
      p.x += p.vx; p.y += p.vy;
      if (p.x < 0 || p.x > W) p.vx *= -1;
      if (p.y < 0 || p.y > H) p.vy *= -1;
      for (var j = i + 1; j < particles.length; j++) {
        var q = particles[j];
        var dx = p.x - q.x, dy = p.y - q.y, d = Math.sqrt(dx * dx + dy * dy);
        if (d < 120) {
          ctx.beginPath(); ctx.moveTo(p.x, p.y); ctx.lineTo(q.x, q.y);
          ctx.strokeStyle = 'rgba(' + p.color + ',' + ((1 - d / 120) * 0.1) + ')';
          ctx.lineWidth = 0.5; ctx.stroke();
        }
      }
      ctx.beginPath(); ctx.arc(p.x, p.y, p.r, 0, 6.28);
      ctx.fillStyle = 'rgba(' + p.color + ',0.4)'; ctx.fill();
    }
    requestAnimationFrame(draw);
  }
  draw();
})();
</script>

<!-- Upload zone interaction -->
<script>
(function() {
  var zone = document.getElementById('upload-zone');
  var defaultState = document.getElementById('upload-default');
  var hoverState = document.getElementById('upload-hover');
  var fileInput = document.getElementById('file-input');
  var pasteArea = document.getElementById('paste-area');
  var scanBtn = document.getElementById('scan-btn');
  var scoreCta = document.getElementById('score-cta');
  var bottomCta = document.getElementById('bottom-cta');

  if (!zone || !fileInput || !pasteArea || !scanBtn) return;

  async function processContent(content) {
    if (!content || !content.trim()) return;
    try { localStorage.setItem('otp_generated_oos', content); } catch(e) {}

    // Show scanning state
    scanBtn.textContent = 'Scanning...';
    scanBtn.disabled = true;
    if (defaultState) defaultState.querySelector('p.text-lg').textContent = 'Scanning your file...';

    try {
      // Call the from-text scanner directly
      var resp = await fetch('/api/v1/scanner/from-text', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ text: content }),
      });
      var data = await resp.json();

      if (data.score) {
        localStorage.setItem('otp_scan_result', JSON.stringify(data));
        window.location.href = '/scan-results';
        return;
      }
    } catch(e) {}

    // Fallback if scanner fails
    window.location.href = '/publish';
  }

  // Drag and drop
  zone.addEventListener('dragover', function(e) {
    e.preventDefault(); e.stopPropagation();
    if (defaultState) defaultState.classList.add('hidden');
    if (hoverState) hoverState.classList.remove('hidden');
  });
  zone.addEventListener('dragleave', function(e) {
    e.preventDefault(); e.stopPropagation();
    if (defaultState) defaultState.classList.remove('hidden');
    if (hoverState) hoverState.classList.add('hidden');
  });
  zone.addEventListener('drop', function(e) {
    e.preventDefault(); e.stopPropagation();
    if (defaultState) defaultState.classList.remove('hidden');
    if (hoverState) hoverState.classList.add('hidden');
    var file = e.dataTransfer.files[0];
    if (file) {
      var reader = new FileReader();
      reader.onload = function(ev) { processContent(ev.target.result); };
      reader.readAsText(file);
    }
  });

  // Click to upload
  zone.addEventListener('click', function(e) {
    if (e.target === pasteArea || e.target === scanBtn || pasteArea.contains(e.target)) return;
    fileInput.click();
  });
  fileInput.addEventListener('change', function() {
    var file = fileInput.files[0];
    if (file) {
      var reader = new FileReader();
      reader.onload = function(ev) { processContent(ev.target.result); };
      reader.readAsText(file);
    }
  });

  // Paste area enable/disable scan button
  pasteArea.addEventListener('input', function() {
    scanBtn.disabled = !pasteArea.value.trim();
  });
  scanBtn.addEventListener('click', function(e) {
    e.stopPropagation();
    processContent(pasteArea.value);
  });

  // Score CTA and bottom CTA scroll to upload zone
  if (scoreCta) {
    scoreCta.addEventListener('click', function(e) {
      e.preventDefault();
      zone.scrollIntoView({ behavior: 'smooth', block: 'center' });
      setTimeout(function() { pasteArea.focus(); }, 600);
    });
  }
  if (bottomCta) {
    bottomCta.addEventListener('click', function(e) {
      e.preventDefault();
      zone.scrollIntoView({ behavior: 'smooth', block: 'center' });
      setTimeout(function() { pasteArea.focus(); }, 600);
    });
  }
})();
</script>

<!-- Score ring animation on scroll -->
<script>
(function() {
  var ring = document.querySelector('.score-ring');
  if (!ring) return;
  var animated = false;
  var observer = new IntersectionObserver(function(entries) {
    if (entries[0].isIntersecting && !animated) {
      animated = true;
      ring.style.transition = 'stroke-dashoffset 2s cubic-bezier(0.16, 1, 0.3, 1)';
      ring.style.strokeDashoffset = '158.3';
      observer.unobserve(ring);
    }
  }, { threshold: 0.3 });

  // Start with full offset (hidden)
  ring.style.strokeDashoffset = '879.6';
  observer.observe(ring);
})();
</script>
