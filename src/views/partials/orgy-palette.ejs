<!-- Orgy brand palette -- now a no-op stub.

     As of 2026-05-28 the orgy palette is promoted into the globally compiled
     stylesheet, so it no longer depends on this per-page include:
       - color utilities (bg-orgy*, text-orgy*, border-orgy*, ring-orgy*)
         are generated from tailwind.config.js
       - the non-tokenizable bits (warm body background, .orgy-mascot-wave
         animation, custom focus-ring box-shadows, .bg-orgy-tint,
         .text-orgy-on-light) live in src/styles/input.css

     This stub is kept so the ~18 pages that still `include` it do not 404.
     New pages do NOT need to include it. Safe to delete once no page references
     it. Do NOT reintroduce a <style> block here -- that was the silent-purge /
     partial-dependency bug this change fixes.

     HTML comments only in this file (EJS <%# %> and <% %> evaluate even inside
     HTML comments and have 500'd /dashboard before -- see memory). -->
