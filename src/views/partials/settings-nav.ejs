<%#
  DEPRECATED (2026-06-14): the in-page settings nav card was replaced by the
  left-rail settings sub-menu (see src/views/layouts/main.ejs -- on /settings/*
  the rail switches to Go Back + settings items, themed orgy-green).

  This partial is intentionally a no-op so the ~10 settings pages that still
  `include('../partials/settings-nav')` keep rendering without a second nav.
  Their flex wrapper now lays out a single full-width content column, which is
  the desired "free up the space" outcome. Safe to delete the includes + unwrap
  the flex wrappers in a later cleanup; blanking here is the one-edit, low-risk
  path that can't 500 a page.
%>
